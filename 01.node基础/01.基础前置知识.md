1.命令行窗口(小黑屏)、CMD窗口、终端、shell
    -搜cmd出来
    -常用指令：
        dir : 列出当前目录下的所有文件
        cd 目录名: 进入到指定的目录
        md 目录名：创建一个文件夹
        rd 目录名：删除一个文件夹
        c：：去C盘
        d: : 去D盘
        e：：去E盘

    -目录
        . 表示当前目录
        .. 表示上一级目录

    -环境变量（Windows系统里的变量）
        path 当我们在命令行窗口打开一个文件，或调用一个程序时，
            系统会首先在当前目录下寻找文件程序，如果找到了则直接打开，
            如果没有找到则会依次到环境变量path路径中找，直到找到为止
            如果没找到则报错
2.进程和线程
    进程
        -进程负责为程序运行提供必备的环境
        -进程相当于工厂的车间

    线程
        -线程是计算机中最小的计算单位，线程负责执行
        -线程相当于工厂中的工人

    单线程
        -JS是单线程
    多线程
3.node执行js文件    node 文件名.js